{
  "name": "bookmarklets",
  "type": "module",
  "scripts": {
    "build": "node bin/build",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "bookmarklet": "^3.0.0"
  },
  "devDependencies": {
    "@openjck/prettier-config": "^1.0.2",
    "prettier": "^3.7.4"
  }
}
